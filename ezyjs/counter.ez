#include "bridge.ez"
n = 1
fn bump(args: [int]) -> int: { global n  n = n + 1  return n }
fn add(args: [int]) -> int:
{
    if args.len() >= 2: { return args[0] + args[1] }
    return 0
}
fn main():
{
    rpc_register("bump", bump)
    rpc_register("add", add)
    rpc_serve()
}
